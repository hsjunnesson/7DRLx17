#pragma once

namespace world {
    /**
     * @brief Dungeon generation thread function
     * 
     * @param data The World to pass in
     * @return int Status
     */
    int dungen_thread(void *data);
}
