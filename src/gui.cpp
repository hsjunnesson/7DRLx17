#include "gui.h"

namespace gui {
    Gui::Gui(Allocator &allocator, SDL_Renderer *renderer)
    : allocator(allocator)
    {}


    void update(Gui &gui, uint32_t t, double dt) {
        
    }

    void render(Gui &gui, SDL_Renderer *renderer) {
        
    }

}